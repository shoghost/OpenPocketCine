name: OpenPocketCine
options:
  bundleIdPrefix: com.opencapture
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true

packages:
  OpenPocketViewCore:
    path: ..

targets:
  OpenPocketCine:
    type: application
    platform: iOS
    sources:
      - OpenPocketCine
    dependencies:
      - package: OpenPocketViewCore
        product: OpenPocketViewCore
    settings:
      base:
        # The app shell (CoreBluetooth / Network / Hotspot glue) uses the Swift 5 language mode to
        # keep the prototype moving; the core package stays Swift 6 strict-concurrency and fully tested.
        # ponytail: tighten to Swift 6 once the I/O layer is validated on hardware.
        SWIFT_VERSION: "5.0"
        # Same App ID as Android and the App Store Connect record. Enable Hotspot
        # Configuration on this identifier in the Developer portal.
        PRODUCT_BUNDLE_IDENTIFIER: com.opencapture.openpocketcine
        PRODUCT_NAME: OpenPocketCine
        TARGETED_DEVICE_FAMILY: "1,2"
        # Do not put XML comments in the entitlements plist. ProcessProductPackaging
        # rewrites it in place (strips comments, retabs) and the build then fails with
        # "Entitlements file was modified during the build".
        CODE_SIGN_ENTITLEMENTS: OpenPocketCine/OpenPocketCine.entitlements
        GCC_PREPROCESSOR_DEFINITIONS: "$(inherited) CI_SILENCE_GL_DEPRECATION=1"
        OTHER_SWIFT_FLAGS: "$(inherited) -Xcc -DCI_SILENCE_GL_DEPRECATION"
        # Same team as OpenZCine — it already has the Hotspot Configuration capability.
        DEVELOPMENT_TEAM: "4V4EGQ6LZA"
        CODE_SIGN_STYLE: Automatic
        GENERATE_INFOPLIST_FILE: YES
        INFOPLIST_KEY_CFBundleDisplayName: OpenPocketCine
        INFOPLIST_KEY_CFBundleName: OpenPocketCine
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        INFOPLIST_KEY_ITSAppUsesNonExemptEncryption: NO
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        INFOPLIST_KEY_UIUserInterfaceStyle: Dark
        INFOPLIST_KEY_UIStatusBarStyle: UIStatusBarStyleLightContent
        INFOPLIST_KEY_UIStatusBarHidden: YES
        # YES so SwiftUI `.statusBarHidden` can hide the iPad system bar after launch.
        # `UIStatusBarHidden~ipad` lives in Info-Frameio.plist — INFOPLIST_KEY cannot
        # spell a `~ipad` variant.
        INFOPLIST_KEY_UIViewControllerBasedStatusBarAppearance: YES
        # Space-separated — a colon list becomes one invalid string and iOS stays portrait-only.
        INFOPLIST_KEY_UISupportedInterfaceOrientations: "UIInterfaceOrientationPortrait UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPad: "UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
        INFOPLIST_KEY_CADisableMinimumFrameDurationOnPhone: YES
        # iPad split-view requires every orientation unless the app is full screen.
        # Live monitor is a single-scene camera UI — not a split-view client.
        INFOPLIST_KEY_UIRequiresFullScreen: YES
        INFOPLIST_KEY_UISupportedInterfaceOrientations: "UIInterfaceOrientationPortrait UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPad: "UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
        INFOPLIST_KEY_NSBluetoothAlwaysUsageDescription: "OpenPocketCine uses Bluetooth to find and pair with your DJI Osmo Pocket and hand off Wi-Fi."
        INFOPLIST_KEY_NSAppTransportSecurity_NSAllowsLocalNetworking: YES
        INFOPLIST_KEY_NSLocalNetworkUsageDescription: "OpenPocketCine talks to your camera over its own Wi-Fi network to read status and video."
        INFOPLIST_KEY_NSPhotoLibraryAddUsageDescription: "OpenPocketCine saves clips you choose to Photos."
        INFOPLIST_KEY_NSAppTransportSecurity_NSAllowsLocalNetworking: YES
        INFOPLIST_FILE: OpenPocketCine/Info-Frameio.plist
    configFiles:
      Debug: OpenPocketCine/Frameio.xcconfig
      Release: OpenPocketCine/Frameio.xcconfig

  OpenPocketCineTest:
    type: application
    platform: iOS
    sources:
      - OpenPocketCine
    dependencies:
      - package: OpenPocketViewCore
        product: OpenPocketViewCore
    settings:
      base:
        SWIFT_VERSION: "5.0"
        PRODUCT_BUNDLE_IDENTIFIER: com.opencapture.openpocketcine.test
        PRODUCT_NAME: OpenPocketCineTest
        TARGETED_DEVICE_FAMILY: "1,2"
        CODE_SIGN_ENTITLEMENTS: OpenPocketCine/OpenPocketCine.entitlements
        GCC_PREPROCESSOR_DEFINITIONS: "$(inherited) CI_SILENCE_GL_DEPRECATION=1"
        OTHER_SWIFT_FLAGS: "$(inherited) -Xcc -DCI_SILENCE_GL_DEPRECATION"
        SWIFT_ACTIVE_COMPILATION_CONDITIONS: "$(inherited) OPENPOCKETCINE_DIAGNOSTICS"
        DEVELOPMENT_TEAM: "4V4EGQ6LZA"
        CODE_SIGN_STYLE: Automatic
        GENERATE_INFOPLIST_FILE: YES
        INFOPLIST_KEY_CFBundleDisplayName: "OpenPocketCine Test"
        INFOPLIST_KEY_CFBundleName: OpenPocketCineTest
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        INFOPLIST_KEY_ITSAppUsesNonExemptEncryption: NO
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        INFOPLIST_KEY_UIUserInterfaceStyle: Dark
        INFOPLIST_KEY_UIStatusBarStyle: UIStatusBarStyleLightContent
        INFOPLIST_KEY_UIStatusBarHidden: YES
        INFOPLIST_KEY_UIViewControllerBasedStatusBarAppearance: YES
        INFOPLIST_KEY_UISupportedInterfaceOrientations: "UIInterfaceOrientationPortrait UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPad: "UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
        INFOPLIST_KEY_CADisableMinimumFrameDurationOnPhone: YES
        INFOPLIST_KEY_UIRequiresFullScreen: YES
        INFOPLIST_KEY_NSBluetoothAlwaysUsageDescription: "OpenPocketCine Test uses Bluetooth to find and pair with your DJI Osmo Pocket and hand off Wi-Fi."
        INFOPLIST_KEY_NSAppTransportSecurity_NSAllowsLocalNetworking: YES
        INFOPLIST_KEY_NSLocalNetworkUsageDescription: "OpenPocketCine Test talks to your camera over its own Wi-Fi network to read status and video."
        INFOPLIST_KEY_NSPhotoLibraryAddUsageDescription: "OpenPocketCine Test saves clips you choose to Photos."
        # Test-only Documents export for control-live.log and live-frame-pacing.csv.
        INFOPLIST_KEY_UIFileSharingEnabled: YES
        INFOPLIST_KEY_LSSupportsOpeningDocumentsInPlace: YES
        INFOPLIST_FILE: OpenPocketCine/Info-Frameio.plist
        # Avoid colliding with the production app's Frame.io callback URL when both are installed.
        FRAMEIO_URL_SCHEME: openpocketcine-test
    configFiles:
      Debug: OpenPocketCine/Frameio.xcconfig
      Release: OpenPocketCine/Frameio.xcconfig

  OpenPocketCineTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - OpenPocketCineTests
    dependencies:
      - target: OpenPocketCine
      - package: OpenPocketViewCore
        product: OpenPocketViewCore
    settings:
      base:
        SWIFT_VERSION: "5.0"
        GENERATE_INFOPLIST_FILE: YES
        PRODUCT_BUNDLE_IDENTIFIER: com.opencapture.openpocketcine.tests
        DEVELOPMENT_TEAM: "4V4EGQ6LZA"
        CODE_SIGN_STYLE: Automatic
        BUNDLE_LOADER: $(TEST_HOST)
        TEST_HOST: $(BUILT_PRODUCTS_DIR)/OpenPocketCine.app/$(BUNDLE_EXECUTABLE_FOLDER_PATH)/OpenPocketCine

  OpenPocketCineTestTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - OpenPocketCineTestTests
    dependencies:
      - target: OpenPocketCineTest
      - package: OpenPocketViewCore
        product: OpenPocketViewCore
    settings:
      base:
        SWIFT_VERSION: "5.0"
        GENERATE_INFOPLIST_FILE: YES
        PRODUCT_BUNDLE_IDENTIFIER: com.opencapture.openpocketcine.test.tests
        DEVELOPMENT_TEAM: "4V4EGQ6LZA"
        CODE_SIGN_STYLE: Automatic
        BUNDLE_LOADER: $(TEST_HOST)
        TEST_HOST: $(BUILT_PRODUCTS_DIR)/OpenPocketCineTest.app/$(BUNDLE_EXECUTABLE_FOLDER_PATH)/OpenPocketCineTest

schemes:
  OpenPocketCine:
    build:
      targets:
        OpenPocketCine: all
        OpenPocketCineTests: [test]
    test:
      targets:
        - OpenPocketCineTests

  OpenPocketCineTest:
    build:
      targets:
        OpenPocketCineTest: all
        OpenPocketCineTests: [test]
        OpenPocketCineTestTests: [test]
    test:
      targets:
        - OpenPocketCineTests
        - OpenPocketCineTestTests
